const config = {
  speed: 20,
  cellSize: 80,
  limitAcc: 1,
  dLimit: 0.8,
  rows: 15,
  cols: 15,
  distToMainWeight: 0.0125,
  maxD: 10,

  // The grid will be centered at this point. Can be any point, even outside the row/col dimensions.
  centerPoint: { x: -2, y: -3 }
};
